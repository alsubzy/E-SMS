
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function SecurityPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Security & Audit</CardTitle>
        <CardDescription>
          View login history, audit trails, and activity logs.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Security and audit features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    