
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function HostelPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Hostel Management</CardTitle>
        <CardDescription>
          Manage hostel rooms, allocation, and fees.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Hostel management features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    