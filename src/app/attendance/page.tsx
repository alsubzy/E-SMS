
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AttendancePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Attendance</CardTitle>
        <CardDescription>
          Track student, teacher, and staff attendance.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Attendance tracking features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    