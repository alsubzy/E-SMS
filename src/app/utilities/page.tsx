
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function UtilitiesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Utilities</CardTitle>
        <CardDescription>
          Access utilities like data import/export and API settings.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Utility features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    