
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function TransportPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Transport Management</CardTitle>
        <CardDescription>
          Manage vehicles, routes, and student transport.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Transport management features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    