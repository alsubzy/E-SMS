
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

export default function AcademicsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Academic Management</CardTitle>
        <CardDescription>
          Manage classes, sections, subjects, and the academic calendar.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Academic management features will be displayed here.</p>
      </CardContent>
    </Card>
  );
}

    